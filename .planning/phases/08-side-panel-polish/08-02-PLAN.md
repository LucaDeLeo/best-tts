---
phase: 08-side-panel-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/styles/shared.css
  - src/lib/styles/theme.css
  - src/popup/styles.css
autonomous: true

must_haves:
  truths:
    - "CSS variables are defined in a shared file importable by popup and side panel"
    - "Dark mode class toggles all colors consistently"
    - "Existing popup UI works with new CSS architecture"
  artifacts:
    - path: "src/lib/styles/shared.css"
      provides: "Shared component styles (buttons, cards, lists)"
    - path: "src/lib/styles/theme.css"
      provides: "CSS custom properties for light/dark themes"
  key_links:
    - from: "src/popup/styles.css"
      to: "src/lib/styles/shared.css"
      via: "@import"
      pattern: "@import.*shared.css"
---

<objective>
Create shared CSS architecture with theme variables.

Purpose: Per CONTEXT.md Decision #10, shared styles ensure visual consistency between popup and side panel. CSS custom properties enable dark mode toggle without duplication.

Output: Shared CSS module with theme variables, refactored popup styles to use shared imports.
</objective>

<execution_context>
@/Users/luca/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-side-panel-polish/CONTEXT.md
@src/popup/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme CSS variables</name>
  <files>src/lib/styles/theme.css</files>
  <action>
Create `src/lib/styles/theme.css` with CSS custom properties for theming:

```css
/* Theme CSS Variables
 * Per CONTEXT.md Decision #3: CSS custom properties with .dark-mode class
 */

:root {
  /* Background colors */
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f5;
  --bg-tertiary: #e5e5e5;
  --bg-highlight: #f0f7ff;
  --bg-warning: #fff3cd;

  /* Text colors */
  --text-primary: #1a1a1a;
  --text-secondary: #525252;
  --text-muted: #737373;
  --text-inverse: #ffffff;

  /* Border colors */
  --border-color: #d4d4d4;
  --border-light: #e5e5e5;
  --border-focus: #3b82f6;

  /* Accent colors */
  --accent-color: #3b82f6;
  --accent-hover: #2563eb;
  --accent-active: #1d4ed8;
  --accent-light: rgba(59, 130, 246, 0.1);

  /* Status colors */
  --success-color: #22c55e;
  --success-bg: #dcfce7;
  --error-color: #ef4444;
  --error-bg: #fef2f2;
  --warning-color: #f59e0b;
  --warning-bg: #fef3c7;

  /* Shadows */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);

  /* Transitions */
  --transition-fast: 0.15s ease;
  --transition-normal: 0.2s ease;
}

/* Dark mode overrides */
:root.dark-mode,
.dark-mode {
  --bg-primary: #1a1a1a;
  --bg-secondary: #2a2a2a;
  --bg-tertiary: #3a3a3a;
  --bg-highlight: #1a2a3a;
  --bg-warning: #3d3200;

  --text-primary: #e5e5e5;
  --text-secondary: #a3a3a3;
  --text-muted: #737373;
  --text-inverse: #1a1a1a;

  --border-color: #404040;
  --border-light: #333333;
  --border-focus: #60a5fa;

  --accent-color: #60a5fa;
  --accent-hover: #3b82f6;
  --accent-active: #2563eb;
  --accent-light: rgba(96, 165, 250, 0.15);

  --success-bg: #14532d;
  --error-bg: #450a0a;
  --warning-bg: #422006;

  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.3);
}

/* System preference support - applied when no explicit preference is set */
@media (prefers-color-scheme: dark) {
  :root:not(.light-mode) {
    --bg-primary: #1a1a1a;
    --bg-secondary: #2a2a2a;
    --bg-tertiary: #3a3a3a;
    --bg-highlight: #1a2a3a;
    --bg-warning: #3d3200;

    --text-primary: #e5e5e5;
    --text-secondary: #a3a3a3;
    --text-muted: #737373;
    --text-inverse: #1a1a1a;

    --border-color: #404040;
    --border-light: #333333;
    --border-focus: #60a5fa;

    --accent-color: #60a5fa;
    --accent-hover: #3b82f6;
    --accent-active: #2563eb;
    --accent-light: rgba(96, 165, 250, 0.15);

    --success-bg: #14532d;
    --error-bg: #450a0a;
    --warning-bg: #422006;

    --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
    --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.3);
  }
}
```
  </action>
  <verify>File exists with both light and dark mode variables defined</verify>
  <done>theme.css has complete CSS custom property definitions for both themes</done>
</task>

<task type="auto">
  <name>Task 2: Create shared component styles</name>
  <files>src/lib/styles/shared.css</files>
  <action>
Create `src/lib/styles/shared.css` with reusable component styles:

```css
/* Shared Component Styles
 * Per CONTEXT.md Decision #10: Shared UI components for popup and side panel
 * Import theme.css first for variable definitions
 */
@import './theme.css';

/* Reset */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* Base typography */
body {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-size: 14px;
  line-height: 1.5;
  color: var(--text-primary);
  background: var(--bg-primary);
}

/* Buttons */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 10px 16px;
  border: none;
  border-radius: 6px;
  font-family: inherit;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-primary {
  background: var(--accent-color);
  color: var(--text-inverse);
}

.btn-primary:hover:not(:disabled) {
  background: var(--accent-hover);
}

.btn-primary:active:not(:disabled) {
  background: var(--accent-active);
}

.btn-secondary {
  background: var(--bg-secondary);
  color: var(--text-secondary);
}

.btn-secondary:hover:not(:disabled) {
  background: var(--bg-tertiary);
}

.btn-tertiary {
  background: transparent;
  color: var(--accent-color);
  border: 1px solid var(--accent-color);
}

.btn-tertiary:hover:not(:disabled) {
  background: var(--accent-light);
}

.btn.small {
  padding: 6px 12px;
  font-size: 12px;
}

.btn.loading::after {
  content: '';
  display: inline-block;
  width: 12px;
  height: 12px;
  margin-left: 8px;
  border: 2px solid currentColor;
  border-top-color: transparent;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* Icon buttons */
.icon-btn {
  background: transparent;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  padding: 6px 10px;
  cursor: pointer;
  color: var(--text-secondary);
  transition: all var(--transition-fast);
}

.icon-btn:hover {
  background: var(--bg-secondary);
  color: var(--text-primary);
}

/* Form elements */
input[type="text"],
input[type="number"],
textarea,
select {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  font-family: inherit;
  font-size: 14px;
  color: var(--text-primary);
  background: var(--bg-primary);
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}

input:focus,
textarea:focus,
select:focus {
  outline: none;
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px var(--accent-light);
}

input::placeholder,
textarea::placeholder {
  color: var(--text-muted);
}

/* Cards */
.card {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 8px;
  padding: 16px;
}

.card-hover:hover {
  border-color: var(--accent-color);
  background: var(--bg-highlight);
}

/* List items */
.list-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 12px;
  border: 1px solid var(--border-light);
  border-radius: 6px;
  background: var(--bg-primary);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.list-item:hover {
  border-color: var(--accent-color);
  background: var(--bg-highlight);
}

.list-item.selected {
  border-color: var(--accent-color);
  background: var(--bg-highlight);
}

.list-item-content {
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.list-item-title {
  font-weight: 500;
  color: var(--text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.list-item-meta {
  font-size: 12px;
  color: var(--text-muted);
}

/* Status badge */
.badge {
  display: inline-flex;
  align-items: center;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
}

.badge-success {
  background: var(--success-bg);
  color: var(--success-color);
}

.badge-error {
  background: var(--error-bg);
  color: var(--error-color);
}

.badge-warning {
  background: var(--warning-bg);
  color: var(--warning-color);
}

/* Messages */
.message {
  font-size: 13px;
  color: var(--text-secondary);
  text-align: center;
}

.message.error {
  color: var(--error-color);
}

.message.success {
  color: var(--success-color);
}

/* Empty state */
.empty-state {
  text-align: center;
  padding: 40px 20px;
  color: var(--text-muted);
}

/* Utility classes */
.hidden {
  display: none !important;
}

.flex {
  display: flex;
}

.flex-1 {
  flex: 1;
}

.gap-2 {
  gap: 8px;
}

.gap-4 {
  gap: 16px;
}
```
  </action>
  <verify>File exists with button, form, card, and list item styles using CSS variables</verify>
  <done>shared.css has complete component styles using theme variables</done>
</task>

<task type="auto">
  <name>Task 3: Update popup styles to use shared CSS</name>
  <files>src/popup/styles.css</files>
  <action>
Refactor `src/popup/styles.css` to import shared styles and remove duplicated definitions.

1. Add import at the top of the file:
```css
@import '../lib/styles/shared.css';
```

2. Remove the following blocks that are now in shared.css:
   - Reset styles (`* { box-sizing... }`)
   - Base body styles
   - Generic .btn styles (keep popup-specific button styles)
   - Basic input/textarea/select styles (keep layout-specific styles)

3. Keep popup-specific styles:
   - .container width (340px)
   - Popup-specific layout (header, main-section)
   - Progress bar styles
   - Control row layouts
   - Library panel specifics

4. Convert hardcoded colors to CSS variables:
   - `#1a1a1a` -> `var(--text-primary)`
   - `#525252` -> `var(--text-secondary)`
   - `#737373` -> `var(--text-muted)`
   - `#3b82f6` -> `var(--accent-color)`
   - `#2563eb` -> `var(--accent-hover)`
   - `#22c55e` -> `var(--success-color)`
   - `#ef4444` -> `var(--error-color)`
   - `#f59e0b` -> `var(--warning-color)`
   - `#ffffff` -> `var(--bg-primary)`
   - `#f5f5f5` -> `var(--bg-secondary)`
   - `#e5e5e5` -> `var(--bg-tertiary)`
   - `#d4d4d4` -> `var(--border-color)`

5. Remove the existing @media (prefers-color-scheme: dark) blocks - theme.css handles this.

Note: Be careful to preserve popup-specific styles. Only remove duplicates that are now in shared.css.
  </action>
  <verify>Popup loads correctly, styles look the same, dark mode works via CSS variables</verify>
  <done>popup/styles.css uses shared imports and CSS variables for colors</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - no CSS errors
2. Load extension in Chrome
3. Open popup - verify it looks the same as before
4. Inspect elements - verify CSS variables are being used
5. Toggle system dark mode - verify colors change
</verification>

<success_criteria>
- src/lib/styles/theme.css exists with light/dark CSS variables
- src/lib/styles/shared.css exists with component styles using variables
- src/popup/styles.css imports shared.css and uses CSS variables
- Popup appearance unchanged but now using CSS variable system
</success_criteria>

<output>
After completion, create `.planning/phases/08-side-panel-polish/08-02-SUMMARY.md`
</output>
